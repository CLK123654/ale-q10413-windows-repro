读取原始建表、种子数据、结算策略和报表布局，核对时间区间、凭证状态、允许类型与租户参数
使用psql把策略载入当前会话，重建settlement模式并保持raw模式三张来源表不变
关联冲销与原凭证，按策略优先级裁决跨租户、冲销链、金额镜像和重复冲销
为受控账本建立同租户引用、冲销形状、金额检查、唯一索引和写入触发器
在三张结算表上启用并强制行级安全，让读取与写入同时受当前租户约束
切换reader和writer角色执行租户读取、空上下文、越权写入与本租户临时写入探针
从数据库对象导出商户结算、冲销判定、访问探针和发布合同，复核四份结果的业务键与金额
